{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "jest --passWithNoTests",
    "clean": "rimraf packages/**/{build,dist}"
  },
  "devDependencies": {
    "@babel/core": "^7.13.8",
    "@babel/preset-env": "^7.13.9",
    "@babel/preset-typescript": "^7.13.0",
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.31",
    "babel-jest": "^26.6.3",
    "jest": "26.6.0"
  }
}
